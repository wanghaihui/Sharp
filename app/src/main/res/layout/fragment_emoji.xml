<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/llEmotion"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <android.support.v4.view.ViewPager android:id="@+id/view_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/flow_indicator"
        android:layout_alignParentTop="true"
        android:layout_marginTop="0dp" />

    <com.conquer.sharp.widget.FlowIndicator
        android:id="@+id/flow_indicator"
        android:layout_width="fill_parent"
        android:layout_height="20dp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="-5dp"
        android:gravity="center"
        app:point_normal_color="@android:color/darker_gray"
        app:point_radius="1dp"
        app:point_selected_color="@android:color/black"
        app:spacing="10dp" />

</RelativeLayout>